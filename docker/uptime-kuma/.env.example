# Copy to .env:  cp .env.example .env

# Uptime Kuma needs no required environment variables out of the box.
# Optional knob (uncomment to use):
#
# Override the listen port inside the container (default 3001).
# UPTIME_KUMA_PORT=3001
