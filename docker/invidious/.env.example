# Copy to .env:  cp .env.example .env
# PostgreSQL password (shared by invidious-db and invidious)
PASSWORD=changeme-db-password
# Random keys (e.g. openssl rand -hex 32)
SERVER_SECRET_KEY=replace-with-random-32-char-key
HMAC_KEY=replace-with-random-string
