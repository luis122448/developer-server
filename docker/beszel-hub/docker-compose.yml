services:
  beszel-hub:
    image: henrygd/beszel:latest
    container_name: beszel-hub
    restart: unless-stopped
    ports:
      - "9000:8090"
    volumes:
      - beszel_data:/beszel_data

volumes:
  beszel_data:
    name: beszel_data
