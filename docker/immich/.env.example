# Copy to .env:  cp .env.example .env
IMMICH_VERSION=release

# Host paths
UPLOAD_LOCATION=/mnt/server/immich/upload
DB_DATA_LOCATION=/mnt/server/immich/postgres

# Database
DB_USERNAME=immich
DB_PASSWORD=changeme
DB_DATABASE_NAME=immich
