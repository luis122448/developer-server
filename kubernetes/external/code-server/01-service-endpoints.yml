apiVersion: v1
kind: Service
metadata:
  name: code-server-external-svc
  namespace: default
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8004
---
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: code-server-external-svc-slice
  namespace: default
  labels:
    kubernetes.io/service-name: code-server-external-svc
addressType: IPv4
ports:
  - name: http
    protocol: TCP
    port: 8004
endpoints:
  - addresses:
      - "192.168.100.161"
