apiVersion: v1
kind: Service
metadata:
  name: trilium-external-svc
  namespace: default
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8009
---
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: trilium-external-svc-slice
  namespace: default
  labels:
    kubernetes.io/service-name: trilium-external-svc
addressType: IPv4
ports:
  - name: http
    protocol: TCP
    port: 8009
endpoints:
  - addresses:
      - "192.168.100.161"
