apiVersion: v1
kind: Namespace
metadata:
  name: vault-production
  labels:
    environment: production
    application: vault
    owner: finance-team
    security-profile: confidential
    monitoring: enabled
  annotations:
    description: "Namespace for Vault in production environment"
    contact-email: "finance-support@your-company.com"
    criticality: "high"
    docs-url: "https://wiki.your-company.com/apps/vault"